package com.crm.travelcrm.communication.enums;

/** Manual triage flag on a conversation. Ordering only — nothing in the system acts on it. */
public enum ConversationPriority {
    LOW,
    NORMAL,
    HIGH,
    URGENT
}
