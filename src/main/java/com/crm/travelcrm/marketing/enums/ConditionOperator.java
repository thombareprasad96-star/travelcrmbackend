package com.crm.travelcrm.marketing.enums;

/** Operators supported by the segment query builder. */
public enum ConditionOperator {
    EQUALS,
    NOT_EQUALS,
    IN,
    NOT_IN,
    CONTAINS,
    GREATER_THAN,
    LESS_THAN,
    BETWEEN,
    IS_SET,
    IS_NOT_SET
}