package com.crm.travelcrm.marketing.enums;

/** Data type of a segmentable customer field — drives the UI input + operator set. */
public enum SegmentFieldType {
    ENUM,
    TEXT,
    DATE,
    NUMBER,
    BOOLEAN,
    MONTH
}